[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pulpwise"
version = "0.1.0"
description = "Content pipeline that pushes articles into Readwise Reader."
readme = "README.md"
requires-python = ">=3.14"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Egor Konovalov" }, { name = "Jesse Read" }]
keywords = ["readwise", "reader", "rss", "read-later", "substack", "newsletters"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: End Users/Desktop",
    "Operating System :: MacOS :: MacOS X",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.14",
    "Topic :: Text Processing :: Markup",
    "Topic :: Internet :: WWW/HTTP :: Indexing/Search",
]

dependencies = [
    "typer>=0.12",
    "httpx>=0.27",
    "feedparser>=6.0.11",
    "lxml>=5.0",
    "tomli-w>=1.0",
    "textual>=0.86",
    "imap-tools>=1.13.0",
]

[project.scripts]
pulpwise = "pulpwise.cli:main"
pw = "pulpwise.cli:main"

[project.urls]
Homepage = "https://github.com/DarthNerdus/pulpwise"
Repository = "https://github.com/DarthNerdus/pulpwise"
Issues = "https://github.com/DarthNerdus/pulpwise/issues"

[dependency-groups]
dev = [
    "mypy>=1.13",
    "pytest>=8.3",
    "pytest-cov>=5.0",
    "ruff>=0.7",
]

[tool.hatch.build.targets.wheel]
packages = ["src/pulpwise"]

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.org/simple"
default = true

[tool.mypy]
strict = true
python_version = "3.14"
files = ["src", "tests"]
warn_unreachable = true
warn_redundant_casts = true

[[tool.mypy.overrides]]
module = ["feedparser", "lxml", "lxml.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = [
    "-ra",
    "--strict-markers",
    "--strict-config",
    "--cov=pulpwise",
    "--cov-report=term-missing",
    "--cov-fail-under=70",
]

[tool.coverage.run]
source = ["src/pulpwise"]
branch = true

[tool.coverage.report]
show_missing = true
fail_under = 70
exclude_also = [
    "if __name__ == .__main__.:",
    "raise NotImplementedError",
]

[tool.ruff]
line-length = 100
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "N", "RUF", "SIM", "PTH", "RET"]
ignore = []

[tool.ruff.lint.flake8-bugbear]
extend-immutable-calls = ["typer.Argument", "typer.Option"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B"]
